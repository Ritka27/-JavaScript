# Лабораторная работа №5: Интерактивные элементы на веб-странице

## Описание проекта
Этот репозиторий содержит реализацию одностраничного сайта для онлайн-обучения программированию. Сайт включает интерактивные элементы: кнопку "Наверх" с плавной прокруткой, галерею с фильтрацией и модальным окном для просмотра изображений, аккордеон, а также карточки контента с hover-эффектами. Всё сделано на чистом HTML, CSS (с Tailwind) и JavaScript.

## Скрининг работы интерактивных элементов
### 1. Кнопка "Наверх"
- **Описание**: Появляется при скролле вниз, плавно возвращает на вершину страницы.
- **Скриншот**:
 <img width="411" height="231" alt="image" src="https://github.com/user-attachments/assets/55d3b9a1-1743-4c28-945a-e0b34cf64c40" />


### 2. Галерея с фильтрацией
- **Описание**: Фильтры по категориям (Python, Web, Mobile); hover-эффекты на фото.
- **Скриншот**:
  <img width="979" height="717" alt="image" src="https://github.com/user-attachments/assets/1080be9b-a2b0-4c02-8de2-218e3e42f892" />

### 3. Модальное окно для фото
- **Описание**: Клик по фото открывает увеличенное изображение на тёмном фоне; закрытие по крестику или Escape.
- **Скриншот**:
<img width="1238" height="674" alt="image" src="https://github.com/user-attachments/assets/064ac819-76d0-4932-9850-e9d730d68ab8" />


### 4. Аккордеон (FAQ)
- **Описание**: Раскрытие/закрытие пунктов по клику; только один открыт за раз.
- **Скриншот**:
 <img width="741" height="259" alt="image" src="https://github.com/user-attachments/assets/91d43c7c-f0e5-4aee-a195-e0cc8e2d26bf" />


## Ответы на вопросы

### Какие интерактивные элементы используются на странице?
На странице реализованы следующие интерактивные элементы:
- **Кнопка "Наверх"**: Фиксированная кнопка с обработчиком `click` для `window.scrollTo({ behavior: 'smooth' })` и `scroll`-событием для показа/скрытия.
- **Фильтры галереи**: Кнопки с `data-category`, обработка `click` для добавления/удаления класса `active` и фильтрации изображений по `data-category`.
- **Модальное окно**: Обработчики `click` на фото для открытия (`classList.remove('hidden')`), на крестик/фон/Escape для закрытия.
- **Аккордеон**

Все элементы используют события DOM (addEventListener) и классы для состояний.
### Что показалось самым интересным и сложным?
- **Самое сложное**: Фильтрация галереи с `data-атрибутами` — пришлось отлаживать, чтобы скрытие (`classList.add('hidden')`) не ломало модалку (клик только на видимых). Также responsive: на мобиле grid сжимается, но фото в модалке адаптируется через `max-w-full`.


### Какие знания получены в ЛЛМ?
- **Основы JS**: Обработка событий (`addEventListener`), манипуляции DOM (`classList`, `getAttribute`), асинхронность.
- **Tailwind CSS**: Arbitrary values (`w-[1000px]`), transitions (`hover:scale-105`), grid/flex для layout.
- **Отладка**: F12 для live-изменений (Styles/Console), фикс ошибок (пути к файлам, опечатки в именах).
- **Дизайн**: Объёмные карточки (shadows, transforms) и UX (плавные анимации, подсветка текста галочками).
- **GitHub**: Загрузка проекта (git add/commit/push).
